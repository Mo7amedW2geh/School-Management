@model Department

@{
    ViewData["Title"] = "Add Department";
}

<h2>Add New Department</h2>

<form asp-action="Create" method="post"> <div class="form-group mt-3">
        <lable asp-for="Name">Name</lable>
        <input asp-for="Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="form-group mt-3">
        <lable>Manger</lable>
        <select asp-for="MangerId" asp-items="ViewBag.Mangers" class="form-control">
            <option value="">-- Select Manger --</option>
        </select>
        <span asp-validation-for="MangerId" class="text-danger"></span>
    </div>

    <div class="form-group mt-3">
        <button type="submit" class="btn btn-primary">Save</button>
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </div>
    
</form>